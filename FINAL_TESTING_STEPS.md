# 实时聊天功能 - 最终验收步骤

## 需要手动运行的命令列表（逐条执行）

### 步骤 1：安装依赖

```bash
cd backend
npm install
```

```bash
cd ../frontend
npm install
```

### 步骤 2：启动后端服务

```bash
cd backend
npm run dev
```

**预期输出：**
```
🚀 Backend server running on http://localhost:5000
🔌 Socket.IO server ready
✅ Server is ready!
```

### 步骤 3：启动前端服务（新终端）

```bash
cd frontend
npm run dev
```

**预期输出：**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

### 步骤 4：准备测试数据（如果还没有）

1. 打开浏览器访问：http://localhost:3000
2. 确保用户1（alice）和用户2（bob）是好友关系
   - 如果不是，先搜索用户并发送/接受好友请求

## 一步步验收路径

### 验收 1：两个浏览器窗口分别登录不同用户

**操作：**
1. 打开浏览器窗口1，访问 http://localhost:3000
2. 点击右上角用户切换器，选择"alice"（用户1）
3. 打开浏览器窗口2（新窗口或新标签），访问 http://localhost:3000
4. 点击右上角用户切换器，选择"bob"（用户2）

**验证点：**
- ✅ 窗口1显示当前用户为"alice"
- ✅ 窗口2显示当前用户为"bob"

### 验收 2：进入同一个私聊对话

**操作：**
1. **窗口1**：点击导航栏"好友列表" → 找到"bob" → 点击"实时聊天"按钮
2. **窗口2**：点击导航栏"好友列表" → 找到"alice" → 点击"实时聊天"按钮

**验证点：**
- ✅ 窗口1显示与"bob"的聊天界面，连接状态为"● 已连接"（绿色）
- ✅ 窗口2显示与"alice"的聊天界面，连接状态为"● 已连接"（绿色）
- ✅ 两个窗口都显示相同的聊天历史（如果有）

### 验收 3：A发消息，B端200ms~1s内收到

**操作：**
1. 在**窗口1（alice）**输入："Hello from alice, testing real-time chat"
2. 点击"发送"按钮或按Enter键

**验证点：**
- ✅ 窗口1立即显示消息（右侧，自己的消息）
- ✅ 窗口2在200ms~1s内显示消息（左侧，对方的消息）
- ✅ 消息内容正确
- ✅ 消息显示时间戳

### 验收 4：B发消息，A端200ms~1s内收到

**操作：**
1. 在**窗口2（bob）**输入："Hi alice, I received your message! This is working!"
2. 点击"发送"按钮或按Enter键

**验证点：**
- ✅ 窗口2立即显示消息（右侧，自己的消息）
- ✅ 窗口1在200ms~1s内显示消息（左侧，对方的消息）
- ✅ 消息内容正确

### 验收 5：消息落库（刷新不丢）

**操作：**
1. 在两个窗口之间发送3-5条消息
2. 在**窗口1**按F5刷新页面
3. 等待页面加载完成

**验证点：**
- ✅ 刷新后所有消息仍然存在
- ✅ 消息顺序正确
- ✅ 消息内容完整
- ✅ 在窗口2刷新，消息也仍然存在

### 验收 6：只有互为好友才能聊天

**操作：**
1. 在窗口1，切换到用户3（charlie，如果存在）
2. 尝试访问好友列表中非好友用户的"实时聊天"

**验证点：**
- ✅ 非好友用户无法建立连接
- ✅ 后端返回错误："You can only chat with accepted friends"

### 验收 7：Socket连接鉴权

**验证点：**
- ✅ 查看后端控制台，应该显示："User X connected"
- ✅ 没有"Authentication error"错误信息
- ✅ 前端连接状态显示"已连接"

### 验收 8：房间ID deterministic

**验证点：**
- ✅ 查看后端控制台，用户1和用户2的roomId应该是"1:2"
- ✅ 无论从哪个用户进入，roomId都相同
- ✅ 双方能互相收到消息（证明在同一房间）

### 验收 9：断线重连

**操作：**
1. 在**后端终端**按Ctrl+C停止服务
2. 观察前端连接状态变化
3. 重新启动后端：`cd backend && npm run dev`
4. 观察前端自动重连

**验证点：**
- ✅ 断线后状态变为"● 未连接"（红色）
- ✅ 重连时状态变为"● 重连中..."（橙色，有动画）
- ✅ 重连成功后状态变为"● 已连接"（绿色）
- ✅ 重连后可以正常发送和接收消息
- ✅ 没有重复消息

### 验收 10：消息去重

**操作：**
1. 在窗口1快速连续发送3条消息："Test 1", "Test 2", "Test 3"
2. 观察窗口2的消息显示

**验证点：**
- ✅ 窗口2只收到3条消息（没有重复）
- ✅ 每条消息只显示一次
- ✅ 消息顺序正确

## 验收通过标准

所有10个验收步骤都应该通过，才能认为功能实现成功。

## 测试完成后

如果所有验收步骤都通过，功能实现成功！🎉

## 额外测试：使用Node.js脚本测试

如果你想用命令行测试Socket连接：

**终端1（用户1）：**
```bash
cd backend
node test-socket.js 1 2
```

**终端2（用户2）：**
```bash
cd backend
node test-socket.js 2 1
```

在两个终端中分别输入消息，观察实时收发效果。

