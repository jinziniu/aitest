# 推荐列表功能使用指南

## 功能概述

推荐列表功能基于相关性评分（Relevance Score）为用户推荐潜在的好友。系统会根据多个维度计算推荐分数，并按分数排序展示。

## 功能特性

### 1. 用户字段扩展
- **gender**: 性别（male/female）
- **birth_year**: 出生年份
- **tags**: 标签数组（如：['tech', 'coding', 'open-source']）

### 2. 相关性评分规则

系统会根据以下规则计算推荐分数（默认50分，范围0-100）：

1. **年龄匹配**（±5岁内）：+10分
2. **标签匹配**：每个匹配标签 +5分
3. **性别互补**：异性 +15分，同性 +5分
4. **Bio关键词匹配**：每个匹配词 +3分

### 3. 事件影响分数

用户操作会影响推荐分数：
- **SKIP**（跳过）：-5分
- **VIEW**（查看）：+2分
- **LIKE**（点赞）：+10分
- **CHAT**（聊天）：+15分

## API 接口

### GET /recommendations

获取推荐列表

**查询参数：**
- `userId` (必需): 当前用户ID
- `gender` (可选): 筛选性别（male/female）
- `age_min` (可选): 最小年龄
- `age_max` (可选): 最大年龄
- `limit` (可选): 返回数量，默认20
- `offset` (可选): 偏移量，默认0

**响应示例：**
```json
{
  "candidates": [
    {
      "id": "2",
      "username": "bob",
      "email": "bob@example.com",
      "gender": "male",
      "birth_year": 1992,
      "tags": ["design", "art", "music"],
      "bio": "UI/UX Designer | Artist | Music lover",
      "relevanceScore": 75,
      "reasons": [
        "年龄相近（相差3岁）",
        "性别互补",
        "共同兴趣：art"
      ]
    }
  ],
  "total": 3,
  "limit": 20,
  "offset": 0
}
```

### POST /recommendations/event

记录推荐事件，更新相关性分数

**请求体：**
```json
{
  "userId": "1",
  "candidateId": "2",
  "eventType": "SKIP"
}
```

**事件类型：**
- `SKIP`: 跳过该推荐
- `VIEW`: 查看用户详情
- `LIKE`: 点赞用户
- `CHAT`: 与AI聊天

**响应示例：**
```json
{
  "success": true,
  "score": 70,
  "reasons": ["年龄相近（相差3岁）", "性别互补"],
  "message": "Score updated to 70"
}
```

## 前端使用

### 1. 访问推荐页面

在导航栏点击"推荐列表"，或直接访问 `/recommendations`

### 2. 筛选推荐

- **性别筛选**：选择"男"、"女"或"全部"
- **年龄范围**：设置最小和最大年龄

点击"应用筛选"按钮应用筛选条件。

### 3. 查看推荐

每个推荐卡片显示：
- 用户基本信息（用户名、性别、年龄、标签、Bio）
- **相关性分数**（0-100）
- **推荐理由**（至少2条）

### 4. 操作按钮

- **Chat with AI**: 跳转到AI聊天页面
- **Add friend**: 跳转到搜索页面添加好友
- **Skip**: 跳过该推荐，分数会降低，列表会刷新

## 验收测试步骤

### 准备：两个不同用户

1. 确保至少有两个用户（例如：用户1和用户2）
2. 确保用户有不同的属性（性别、年龄、标签）

### 测试1：不同用户看到不同推荐

**步骤：**
1. 在用户切换器中选择"用户1"
2. 访问"推荐列表"页面
3. 记录推荐的用户和分数
4. 切换到"用户2"
5. 访问"推荐列表"页面
6. 对比两个用户的推荐列表

**预期结果：**
- 两个用户看到的推荐列表不同
- 推荐顺序按相关性分数降序排列
- 每个推荐至少显示2条推荐理由

### 测试2：筛选功能

**步骤：**
1. 选择用户1
2. 访问推荐列表
3. 设置性别筛选为"女"
4. 点击"应用筛选"
5. 验证列表只显示女性用户
6. 设置年龄范围（例如：25-35岁）
7. 点击"应用筛选"
8. 验证列表只显示符合年龄范围的用户

**预期结果：**
- 筛选后只显示符合条件的用户
- 筛选后的列表仍然按分数排序

### 测试3：分数变化

**步骤：**
1. 选择用户1
2. 访问推荐列表
3. 记录用户2的初始分数
4. 点击用户2的"Skip"按钮
5. 等待列表刷新
6. 查看用户2的新分数

**预期结果：**
- Skip后分数降低（约-5分）
- 推荐理由中包含"曾被跳过"
- 列表重新排序

### 测试4：推荐理由

**步骤：**
1. 选择用户1
2. 访问推荐列表
3. 查看每个推荐的"推荐理由"部分

**预期结果：**
- 每个推荐至少显示2条理由
- 理由清晰说明为什么推荐该用户
- 理由可能包括：年龄相近、性别互补、共同兴趣、简介相似等

### 测试5：相关性计算

**步骤：**
1. 创建两个用户：
   - 用户A：性别=女，出生年份=1995，标签=['tech', 'coding']
   - 用户B：性别=男，出生年份=1992，标签=['tech', 'design']
2. 以用户A身份查看推荐
3. 查看用户B的推荐分数和理由

**预期结果：**
- 用户B应该出现在推荐列表中
- 分数应该反映：年龄相近（+10）、性别互补（+15）、共同标签tech（+5）
- 推荐理由应该包含这些信息

## 数据库结构

### RelevanceScore 表

存储在 `backend/data.json` 的 `relevanceScores` 数组中：

```json
{
  "viewerId": "1",
  "candidateId": "2",
  "score": 75,
  "reasons": ["年龄相近（相差3岁）", "性别互补"],
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

**唯一键：** (viewerId, candidateId)

## 常见问题

### Q: 为什么推荐列表为空？
A: 可能原因：
- 所有用户都已经是好友
- 筛选条件太严格，没有符合条件的用户
- 数据库中用户数量不足

### Q: 分数如何更新？
A: 分数在以下情况更新：
- 首次查看推荐时自动计算
- 用户执行操作（Skip/View/Like/Chat）时更新
- 每次访问推荐列表时重新计算基础分数

### Q: 如何重置推荐分数？
A: 目前没有重置功能。可以通过删除 `backend/data.json` 中的 `relevanceScores` 数组来重置所有分数。

## 技术实现

- **后端**: `backend/recommendations.js` - 推荐逻辑和评分计算
- **数据库**: `backend/db.js` - RelevanceScore 表操作
- **API**: `backend/index.js` - REST API 端点
- **前端**: `frontend/src/pages/Recommendations.tsx` - 推荐页面组件

