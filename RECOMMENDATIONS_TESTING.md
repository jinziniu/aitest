# 推荐列表功能验收测试

## 测试目标

验证两个不同用户登录后，能够看到不同的推荐列表，并且推荐列表按相关性分数排序。

## 前置准备

### 1. 确保后端和前端都已启动

```bash
# 终端1：启动后端
cd backend
npm start

# 终端2：启动前端
cd frontend
npm run dev
```

### 2. 准备测试数据

确保数据库中有至少3个用户（不包括当前用户），且用户属性不同：

- **用户1 (alice)**: 性别=女，出生年份=1995，标签=['tech', 'coding', 'open-source']
- **用户2 (bob)**: 性别=男，出生年份=1992，标签=['design', 'art', 'music']
- **用户3 (charlie)**: 性别=男，出生年份=1990，标签=['travel', 'photography', 'adventure']
- **用户4 (david)**: 性别=男，出生年份=1988，标签=['fitness', 'health', 'nutrition']

如果数据不存在，可以通过"用户管理"页面创建或编辑用户。

## 测试场景1：不同用户看到不同推荐

### 步骤

1. **打开浏览器窗口1**
   - 访问 http://localhost:3000
   - 在右上角用户切换器中选择"用户1 (alice)"
   - 点击导航栏"推荐列表"

2. **记录用户1的推荐列表**
   - 记录推荐的用户顺序
   - 记录每个推荐的相关性分数
   - 记录每个推荐的推荐理由

3. **打开浏览器窗口2（或新标签页）**
   - 访问 http://localhost:3000
   - 在右上角用户切换器中选择"用户2 (bob)"
   - 点击导航栏"推荐列表"

4. **记录用户2的推荐列表**
   - 记录推荐的用户顺序
   - 记录每个推荐的相关性分数
   - 记录每个推荐的推荐理由

5. **对比两个推荐列表**
   - 对比推荐顺序是否不同
   - 对比分数是否不同
   - 对比推荐理由是否不同

### 预期结果

✅ **用户1 (alice) 的推荐：**
- 应该看到用户2、用户3、用户4（排除自己）
- 用户2的分数应该较高（因为：年龄相近1995-1992=3岁 +10，性别互补 +15，可能有共同标签）
- 推荐理由应该包含"年龄相近"、"性别互补"等

✅ **用户2 (bob) 的推荐：**
- 应该看到用户1、用户3、用户4（排除自己）
- 用户1的分数应该较高（因为：年龄相近1992-1995=3岁 +10，性别互补 +15）
- 推荐理由应该包含"年龄相近"、"性别互补"等

✅ **两个列表应该不同：**
- 推荐顺序不同（因为相关性分数不同）
- 分数不同（因为viewer不同，计算规则相同但结果不同）
- 推荐理由可能不同（因为匹配的维度不同）

## 测试场景2：筛选功能

### 步骤

1. **选择用户1 (alice)**
   - 访问推荐列表页面

2. **应用性别筛选**
   - 在筛选区域选择"性别：女"
   - 点击"应用筛选"
   - 验证列表只显示女性用户

3. **应用年龄筛选**
   - 设置"年龄范围：25-35岁"
   - 点击"应用筛选"
   - 验证列表只显示年龄在25-35岁之间的用户

4. **清除筛选**
   - 将性别改为"全部"
   - 清空年龄范围
   - 点击"应用筛选"
   - 验证列表显示所有符合条件的用户

### 预期结果

✅ 筛选后只显示符合条件的用户
✅ 筛选后的列表仍然按相关性分数降序排列
✅ 每个推荐仍然显示推荐理由

## 测试场景3：分数变化（Skip操作）

### 步骤

1. **选择用户1 (alice)**
   - 访问推荐列表页面
   - 找到用户2 (bob)的推荐卡片
   - 记录用户2的初始分数（例如：75分）

2. **执行Skip操作**
   - 点击用户2卡片上的"Skip"按钮
   - 等待列表自动刷新

3. **验证分数变化**
   - 查看用户2的新分数（应该降低约5分，例如：70分）
   - 查看推荐理由中是否包含"曾被跳过"

4. **验证排序变化**
   - 如果用户2的分数降低，可能影响排序
   - 验证列表仍然按分数降序排列

### 预期结果

✅ Skip后分数降低（约-5分）
✅ 推荐理由中包含"曾被跳过"
✅ 列表重新排序（如果分数变化导致顺序改变）
✅ 列表自动刷新

## 测试场景4：推荐理由验证

### 步骤

1. **选择用户1 (alice)**
   - 访问推荐列表页面

2. **检查每个推荐的推荐理由**
   - 验证每个推荐至少显示2条理由
   - 验证理由清晰说明为什么推荐该用户

3. **验证理由的准确性**
   - 如果显示"年龄相近"，验证年龄差是否在±5岁内
   - 如果显示"共同兴趣"，验证标签是否真的有重叠
   - 如果显示"性别互补"，验证性别是否不同

### 预期结果

✅ 每个推荐至少显示2条理由
✅ 理由准确反映推荐原因
✅ 理由格式清晰易读

## 测试场景5：相关性计算验证

### 步骤

1. **创建测试用户**
   - 用户A：性别=女，出生年份=1995，标签=['tech', 'coding']
   - 用户B：性别=男，出生年份=1992，标签=['tech', 'design']

2. **以用户A身份查看推荐**
   - 切换到用户A
   - 访问推荐列表
   - 查看用户B的推荐分数和理由

3. **手动计算预期分数**
   - 基础分数：50
   - 年龄相近（1995-1992=3岁）：+10
   - 性别互补：+15
   - 共同标签tech：+5
   - **预期总分：80**

4. **验证实际分数**
   - 查看用户B的实际分数
   - 对比预期分数和实际分数（允许±5分的误差）

### 预期结果

✅ 实际分数接近预期分数
✅ 推荐理由包含所有匹配的维度
✅ 分数在合理范围内（0-100）

## 常见问题排查

### Q: 推荐列表为空
**可能原因：**
- 所有用户都已经是好友（系统会排除好友）
- 筛选条件太严格
- 数据库中用户数量不足

**解决方法：**
- 检查好友列表，确保有非好友用户
- 放宽筛选条件
- 通过"用户管理"创建更多用户

### Q: 两个用户看到的推荐相同
**可能原因：**
- 用户属性完全相同
- 相关性计算逻辑有问题

**解决方法：**
- 确保两个用户的属性不同（性别、年龄、标签）
- 检查后端日志，查看推荐计算过程

### Q: 分数不更新
**可能原因：**
- 后端服务未重启
- 数据库文件权限问题

**解决方法：**
- 重启后端服务
- 检查 `backend/data.json` 文件权限

## 验收标准

✅ **必须满足：**
1. 两个不同用户登录后，看到不同的推荐列表
2. 推荐列表按相关性分数降序排列
3. 每个推荐至少显示2条推荐理由
4. 筛选功能正常工作
5. Skip操作能正确更新分数并刷新列表

✅ **应该满足：**
1. 推荐理由准确反映推荐原因
2. 分数计算符合预期规则
3. UI界面美观易用
4. 操作响应及时

## 测试完成检查清单

- [ ] 测试场景1：不同用户看到不同推荐 ✅
- [ ] 测试场景2：筛选功能 ✅
- [ ] 测试场景3：分数变化（Skip操作） ✅
- [ ] 测试场景4：推荐理由验证 ✅
- [ ] 测试场景5：相关性计算验证 ✅

## 验收通过标准

所有测试场景通过，且满足以下条件：
- 功能完整可用
- 界面友好易用
- 性能满足要求（列表加载<2秒）
- 无明显bug

