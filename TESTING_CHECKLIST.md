# 实时聊天功能测试检查清单

## 需要手动运行的命令列表

### 1. 安装依赖

```bash
# 后端依赖
cd backend
npm install

# 前端依赖
cd ../frontend
npm install
```

### 2. 启动服务

**终端1 - 后端服务：**
```bash
cd backend
npm run dev
```

**终端2 - 前端服务：**
```bash
cd frontend
npm run dev
```

### 3. 准备测试数据（如果还没有）

确保至少有两个用户并且是好友关系：
- 用户1（alice, ID: 1）
- 用户2（bob, ID: 2）

如果还没有建立好友关系：
1. 访问 http://localhost:3000
2. 使用用户1登录
3. 搜索用户2，发送好友请求
4. 切换到用户2，接受好友请求

## 一步步验收路径

### 验收步骤 1：两个浏览器窗口分别登录不同用户

1. **打开浏览器窗口1**
   - 访问：http://localhost:3000
   - 点击右上角用户切换器
   - 选择"alice"（用户1）
   - ✅ 验证：页面显示当前用户为"alice"

2. **打开浏览器窗口2**
   - 访问：http://localhost:3000（或新标签页）
   - 点击右上角用户切换器
   - 选择"bob"（用户2）
   - ✅ 验证：页面显示当前用户为"bob"

### 验收步骤 2：进入同一个私聊对话

1. **窗口1操作**：
   - 点击导航栏"好友列表"
   - 在"我的好友"区域找到"bob"
   - 点击"实时聊天"按钮
   - ✅ 验证：进入聊天页面，显示"bob"的头像和用户名
   - ✅ 验证：连接状态显示"● 已连接"（绿色）

2. **窗口2操作**：
   - 点击导航栏"好友列表"
   - 在"我的好友"区域找到"alice"
   - 点击"实时聊天"按钮
   - ✅ 验证：进入聊天页面，显示"alice"的头像和用户名
   - ✅ 验证：连接状态显示"● 已连接"（绿色）

### 验收步骤 3：A发消息，B端200ms~1s内收到

1. **窗口1（alice）发送消息**：
   - 在输入框输入："Hello from alice, this is a test message"
   - 点击"发送"按钮或按Enter
   - ✅ 验证：消息立即显示在窗口1的右侧（自己的消息）
   - ✅ 验证：消息显示时间戳

2. **窗口2（bob）观察**：
   - ✅ 验证：在200ms~1s内，消息出现在窗口2的左侧（对方的消息）
   - ✅ 验证：消息内容正确："Hello from alice, this is a test message"
   - ✅ 验证：消息显示时间戳

### 验收步骤 4：B发消息，A端200ms~1s内收到

1. **窗口2（bob）发送消息**：
   - 在输入框输入："Hi alice, I received your message!"
   - 点击"发送"按钮或按Enter
   - ✅ 验证：消息立即显示在窗口2的右侧（自己的消息）

2. **窗口1（alice）观察**：
   - ✅ 验证：在200ms~1s内，消息出现在窗口1的左侧（对方的消息）
   - ✅ 验证：消息内容正确："Hi alice, I received your message!"

### 验收步骤 5：消息落库（刷新不丢）

1. **发送多条消息**：
   - 在窗口1发送："Message 1"
   - 在窗口2发送："Message 2"
   - 在窗口1发送："Message 3"
   - ✅ 验证：所有消息在两个窗口都正确显示

2. **刷新页面**：
   - 在窗口1按F5刷新页面
   - ✅ 验证：页面重新加载后，所有消息仍然存在
   - ✅ 验证：消息顺序正确
   - ✅ 验证：消息内容完整

3. **再次刷新**：
   - 在窗口2按F5刷新页面
   - ✅ 验证：所有消息仍然存在

### 验收步骤 6：只有互为好友才能聊天

1. **测试非好友用户**：
   - 在窗口1，切换到用户3（charlie）
   - 尝试访问：http://localhost:3000/chat/private/1
   - ✅ 验证：应该被拒绝或重定向（如果charlie和alice不是好友）

2. **建立好友关系后**：
   - 确保charlie和alice是好友（accepted状态）
   - 再次尝试进入聊天
   - ✅ 验证：可以正常进入聊天页面

### 验收步骤 7：Socket连接鉴权

1. **检查后端日志**：
   - 查看后端控制台
   - ✅ 验证：连接时显示"User X connected"
   - ✅ 验证：没有"Authentication error"错误

2. **测试无效token**：
   - 修改前端代码，使用无效userId
   - ✅ 验证：连接应该被拒绝

### 验收步骤 8：房间ID deterministic

1. **检查房间ID**：
   - 查看后端控制台日志
   - ✅ 验证：用户1和用户2的roomId应该是"1:2"（排序后）
   - ✅ 验证：无论从哪个用户进入，roomId都相同

2. **验证双方在同一房间**：
   - 在窗口1发送消息
   - ✅ 验证：窗口2能收到（说明在同一房间）

### 验收步骤 9：断线重连

1. **模拟断线**：
   - 停止后端服务（在终端1按Ctrl+C）
   - ✅ 验证：前端连接状态变为"● 未连接"（红色）

2. **自动重连**：
   - 重新启动后端服务：`cd backend && npm run dev`
   - ✅ 验证：前端连接状态变为"● 重连中..."（橙色，有动画）
   - ✅ 验证：几秒后状态变为"● 已连接"（绿色）

3. **重连后功能**：
   - 发送一条消息测试
   - ✅ 验证：消息能正常发送和接收
   - ✅ 验证：没有重复消息

### 验收步骤 10：消息去重

1. **快速发送多条消息**：
   - 在窗口1快速连续发送3条消息
   - ✅ 验证：窗口2只收到3条消息（没有重复）

2. **重连后测试**：
   - 断开连接后重连
   - ✅ 验证：历史消息没有重复显示

## 验收通过标准

所有10个验收步骤都应该通过，才能认为功能实现成功。

## 常见问题快速排查

| 问题 | 可能原因 | 解决方案 |
|------|---------|---------|
| Socket连接失败 | 后端未启动 | 检查后端服务是否运行 |
| 消息发送失败 | 未连接或非好友 | 检查连接状态和好友关系 |
| 消息重复 | 去重逻辑问题 | 检查clientMsgId和serverMsgId |
| 消息丢失 | 数据库写入失败 | 检查data.json文件权限 |
| CORS错误 | CORS配置问题 | 检查后端CORS设置 |

## 测试完成后

如果所有验收步骤都通过，功能实现成功！🎉

