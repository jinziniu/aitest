# 用户切换功能使用指南

## 问题说明

之前系统中，所有用户都硬编码为"用户1"，导致：
- 用户A可以向用户B发送好友请求
- 但用户B无法看到和接受这个请求（因为前端始终以用户1的身份运行）

## 解决方案

现在添加了**用户切换功能**，允许用户切换当前登录的账户。

## 如何使用

### 1. 查看当前用户

在导航栏右侧，可以看到当前登录的用户信息：
- 显示格式：`用户名 (邮箱)`
- 如果未选择用户，默认显示：`用户 1`

### 2. 切换用户

1. **点击导航栏右侧的用户信息按钮**（显示当前用户名的按钮）
2. **在下拉菜单中选择要切换的用户**
3. **页面会自动刷新**，所有功能都会以新用户身份运行

### 3. 接受好友请求的完整流程

#### 场景：用户1（alice）向用户2（bob）发送好友请求

**步骤1：用户1发送请求**
1. 确保当前用户是"用户1"（alice）
2. 进入"搜索用户"页面
3. 搜索"bob"
4. 点击"添加好友"按钮
5. 系统提示"已向 bob 发送好友请求"

**步骤2：用户2接受请求**
1. **点击导航栏右侧的用户信息按钮**
2. **在下拉菜单中选择"bob"**
3. 页面刷新后，进入"好友列表"页面
4. 在"待处理的好友请求"区域，可以看到来自"alice"的请求
5. **点击"接受"按钮**
6. 请求被接受，alice会出现在"我的好友"区域

### 4. 退出登录

1. 点击导航栏右侧的用户信息按钮
2. 在下拉菜单底部点击"退出登录"
3. 系统会切换回默认用户（用户1）

## 功能特性

- ✅ **用户状态持久化**：选择的用户会保存在浏览器localStorage中，刷新页面后仍然有效
- ✅ **全局生效**：切换用户后，所有页面（搜索、好友列表、聊天等）都会以新用户身份运行
- ✅ **实时切换**：切换用户后页面自动刷新，确保所有组件使用新的用户ID
- ✅ **用户列表**：下拉菜单显示所有系统中的用户，方便选择

## 技术实现

- 使用 React Context (`UserContext`) 管理当前用户状态
- 用户信息保存在 localStorage 中
- 所有页面通过 `useUser()` hook 获取当前用户ID
- 切换用户时自动刷新页面以确保状态同步

## 注意事项

- 切换用户后页面会刷新，这是为了确保所有组件都使用新的用户ID
- 如果localStorage被清除，系统会恢复为默认用户（用户1）
- 用户切换功能不影响后端数据，只是改变前端的"当前用户"身份

